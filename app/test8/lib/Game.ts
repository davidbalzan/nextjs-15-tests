export interface Game {
    // Define the properties of the Game interface based on the expected response
    id: string;
    name: string;
    releaseDate: string;
    // Add other properties as needed
}